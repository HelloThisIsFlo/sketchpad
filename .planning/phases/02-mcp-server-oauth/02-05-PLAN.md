---
phase: 02-mcp-server-oauth
plan: 05
type: execute
wave: 2
depends_on:
  - "02-04"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
  - MCP-05
  - TOOL-01
  - TOOL-02

must_haves:
  truths:
    - "test-oauth.sh Steps 1-3 pass automatically (discovery, 401, DCR)"
    - "test-oauth.sh Steps 4-7 pass with human completing GitHub login"
    - "All 17 Phase 2 requirements confirmed at runtime"
  artifacts: []
  key_links:
    - from: "test-oauth.sh"
      to: "http://localhost:8000 (or tunnel URL)"
      via: "curl requests against running server"
      pattern: "PASS_COUNT.*FAIL_COUNT"
---

<objective>
Human-verify the full OAuth flow and MCP tool calls by running test-oauth.sh end-to-end.

Purpose: 02-VERIFICATION.md documents that Steps 4-7 of test-oauth.sh (GitHub OAuth browser flow, token exchange, refresh token, MCP tool calls) were never executed at runtime. The code is structurally correct but these 13 requirements (AUTH-02 through AUTH-07, MCP-01 through MCP-05, TOOL-01, TOOL-02) are unverified. Additionally, DISC-02 needs runtime confirmation after Plan 04's fix.

Output: All 17 Phase 2 requirements confirmed at runtime via test-oauth.sh
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mcp-server-oauth/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start server with cloudflared tunnel</name>
  <files></files>
  <action>
1. Ensure .env file exists with valid GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, JWT_SIGNING_KEY, STORAGE_ENCRYPTION_KEY, and SERVER_URL values. If .env is missing, check if K8s secrets are available:
   ```bash
   kubectl get secret sketchpad-github-oauth -n sketchpad -o jsonpath='{.data.client-id}' | base64 -d
   kubectl get secret sketchpad-github-oauth -n sketchpad -o jsonpath='{.data.client-secret}' | base64 -d
   ```
   Generate JWT_SIGNING_KEY and STORAGE_ENCRYPTION_KEY if not already present.

2. Start the server:
   ```bash
   uv run python -m sketchpad &
   ```

3. Start cloudflared quick tunnel:
   ```bash
   cloudflared tunnel --url http://localhost:8000
   ```
   Note the tunnel URL (*.trycloudflare.com).

4. Update SERVER_URL in .env with the tunnel URL.

5. Restart the server with the updated SERVER_URL.

6. Quick smoke test -- run Steps 1-3 of test-oauth.sh:
   ```bash
   # Just test that discovery, 401, and DCR work before asking user to do the browser flow
   ```
   Confirm Steps 1-3 pass. If DISC-02 still fails, investigate further before proceeding.

IMPORTANT: The GitHub OAuth App callback URL must be updated to match the new tunnel URL.
Print a clear message: "Update GitHub OAuth App callback URL to: {tunnel_url}/auth/callback"
  </action>
  <verify>
    <automated>curl -sf http://localhost:8000/.well-known/oauth-authorization-server | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'authorization_endpoint' in d; print('PASS')"</automated>
  </verify>
  <done>Server is running, cloudflared tunnel active, Steps 1-3 of test-oauth.sh pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Server running with cloudflared tunnel and corrected DISC-02 URL. Steps 1-3 pass automatically. Now the user needs to complete the GitHub OAuth browser flow (Steps 4-7).</what-built>
  <how-to-verify>
1. The executor will print a message showing the GitHub OAuth App callback URL that needs updating. Go to https://github.com/settings/developers, find the Sketchpad OAuth App, and update the "Authorization callback URL" to the tunnel URL + /auth/callback.

2. Run the full test script:
   ```bash
   ./test-oauth.sh https://YOUR-TUNNEL-URL.trycloudflare.com
   ```

3. At Step 4, the script prints an authorization URL. Open it in your browser.

4. Complete GitHub login. You will be redirected to localhost:9999/callback (which will fail to load -- that is expected). Copy the `code` parameter from the URL bar and paste it into the terminal.

5. Watch Steps 5-7 execute:
   - Step 5 (Token Exchange): Should show access_token, refresh_token, expires_in
   - Step 6 (Refresh Token): Should show new access_token
   - Step 7 (MCP Tool Calls): Should show initialize PASS, tools/list with 2 tools, read_file PASS (welcome message), write_file PASS, read_file confirms content PASS

6. Expected final output: "ALL CHECKS PASSED"

If any step fails, describe the error output.
  </how-to-verify>
  <resume-signal>Type "all passed" with the count (e.g., "all passed - 15/15 checks"), or describe failures</resume-signal>
</task>

</tasks>

<verification>
- test-oauth.sh exits with code 0 ("ALL CHECKS PASSED")
- Every Phase 2 requirement has been exercised at runtime
</verification>

<success_criteria>
- DISC-01: /.well-known/oauth-authorization-server returns JSON with endpoints (Step 1)
- DISC-02: /.well-known/oauth-protected-resource/mcp returns JSON with resource field (Step 1)
- DISC-03: POST /mcp without token returns 401 with WWW-Authenticate (Step 2)
- AUTH-01: POST /register returns client_id (Step 3)
- AUTH-02: /authorize redirects to GitHub (Step 4)
- AUTH-03: GitHub callback redirects with auth code (Step 4)
- AUTH-04: /token with PKCE returns access_token (Step 5)
- AUTH-05: Token response includes refresh_token (Step 5)
- AUTH-06: grant_type=refresh_token returns new tokens (Step 6)
- AUTH-07: Token response includes expires_in (Step 5)
- MCP-01: POST /mcp with token accepts Streamable HTTP (Step 7a)
- MCP-02: initialize returns protocolVersion (Step 7a)
- MCP-03: tools/list returns read_file and write_file (Step 7b)
- MCP-04: tools/call dispatches correctly (Steps 7c-7e)
- MCP-05: Bearer token validated (Step 2 verifies 401; Steps 7a-7e verify authenticated path)
- TOOL-01: read_file returns content (Step 7c)
- TOOL-02: write_file updates file and read_file confirms (Steps 7d-7e)
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-server-oauth/02-05-SUMMARY.md`
</output>
