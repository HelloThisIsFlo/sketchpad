---
phase: 07-build-tooling-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .github/workflows/build.yaml
  - Makefile
autonomous: true
requirements:
  - BUILD-02

must_haves:
  truths:
    - "CI pipeline runs: checkout -> setup-just -> setup-uv -> uv sync -> just test -> just lint -> Docker login -> Docker build+push"
    - "Makefile no longer exists in the repository"
    - "CI uses `extractions/setup-just@v3` with version pin `just-version: '1'`"
    - "CI uses `astral-sh/setup-uv@v7` with `python-version: '3.12'` and cache enabled"
  artifacts:
    - path: ".github/workflows/build.yaml"
      provides: "Updated CI pipeline with just + test/lint gates"
      contains: "setup-just"
  key_links:
    - from: ".github/workflows/build.yaml"
      to: "Justfile"
      via: "just test and just lint steps"
      pattern: "just (test|lint)"
    - from: ".github/workflows/build.yaml"
      to: "extractions/setup-just@v3"
      via: "uses directive"
      pattern: "extractions/setup-just@v3"
    - from: ".github/workflows/build.yaml"
      to: "astral-sh/setup-uv@v7"
      via: "uses directive"
      pattern: "astral-sh/setup-uv@v7"
---

<objective>
Update the GitHub Actions CI workflow to use just for test/lint gating before Docker build+push, then delete the Makefile.

Purpose: CI now gates on tests and lint passing before building the container image. The Makefile is removed since the Justfile fully replaces it.
Output: Updated CI workflow, deleted Makefile
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-tooling-migration/07-CONTEXT.md
@.planning/phases/07-build-tooling-migration/07-RESEARCH.md
@.planning/phases/07-build-tooling-migration/07-01-SUMMARY.md

@.github/workflows/build.yaml
@Justfile
</context>

<interfaces>
<!-- Existing CI workflow structure the executor needs to modify -->

From .github/workflows/build.yaml:
- Job: `build-and-push` on `ubuntu-latest`
- Trigger: `push` to `main`
- Env: `REGISTRY: ghcr.io`, `IMAGE_NAME: hellothisisflo/sketchpad`
- Permissions: `contents: read`, `packages: write`
- Current steps: checkout -> docker login -> metadata -> build+push
- New steps to insert between checkout and docker login: setup-just, setup-uv, uv sync, just test, just lint
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update CI workflow with just + test/lint gates</name>
  <files>.github/workflows/build.yaml</files>
  <action>
Edit `.github/workflows/build.yaml` to insert new steps between "Checkout repository" and "Log in to GitHub Container Registry". Keep the existing header comment, name, trigger, env, permissions, and Docker steps unchanged.

The full step order after editing must be:

1. **Checkout repository** (existing — `actions/checkout@v4`)
2. **Install just** (NEW)
   ```yaml
   - name: Install just
     uses: extractions/setup-just@v3
     with:
       just-version: '1'
   ```
3. **Install uv** (NEW)
   ```yaml
   - name: Install uv
     uses: astral-sh/setup-uv@v7
     with:
       python-version: "3.12"
       enable-cache: true
   ```
4. **Install dependencies** (NEW)
   ```yaml
   - name: Install dependencies
     run: uv sync --locked --dev
   ```
5. **Run tests** (NEW)
   ```yaml
   - name: Run tests
     run: just test
   ```
6. **Run linter** (NEW)
   ```yaml
   - name: Run linter
     run: just lint
   ```
7. **Log in to GitHub Container Registry** (existing — unchanged)
8. **Extract metadata** (existing — unchanged)
9. **Build and push Docker image** (existing — unchanged)

This ensures tests and lint MUST pass before Docker build+push runs (per user decision). Pin setup-just to version '1' (not '*') to avoid CI breakage on future just releases (per pitfall #5). Use `uv sync --locked --dev` for explicit dependency install before `just test` / `just lint` (per pitfall #3).

After editing, validate the YAML syntax with: `python -c "import yaml; yaml.safe_load(open('.github/workflows/build.yaml'))"`
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Agentic/Capacities/sketchpad && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build.yaml')); steps=[s['name'] for s in d['jobs']['build-and-push']['steps']]; assert 'Install just' in steps and 'Run tests' in steps and 'Run linter' in steps, f'Missing steps: {steps}'; print('CI workflow valid. Steps:', steps)"</automated>
  </verify>
  <done>CI workflow has 9 steps in correct order. setup-just@v3 with version pin '1'. setup-uv@v7 with python 3.12 and cache. uv sync before test/lint. Test and lint gates before Docker build+push.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Makefile</name>
  <files>Makefile</files>
  <action>
Delete the `Makefile` from the project root. It is fully replaced by the Justfile created in plan 07-01.

Run `just --list` one more time to confirm the Justfile is still the active command runner. Run `just test` to confirm tests pass. Verify the Makefile is gone with `ls Makefile` (should fail).

This is the final cleanup step — no stale build file left behind (per success criteria #4 in ROADMAP).
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Agentic/Capacities/sketchpad && test ! -f Makefile && just --list && just test</automated>
  </verify>
  <done>Makefile is deleted. `just --list` shows all recipes. `just test` passes. No stale build files remain.</done>
</task>

</tasks>

<verification>
- `.github/workflows/build.yaml` contains setup-just, setup-uv, uv sync, just test, just lint steps before Docker build+push
- Step order matches: checkout -> setup-just -> setup-uv -> uv sync -> just test -> just lint -> docker login -> metadata -> build+push
- `Makefile` does not exist
- `just --list` and `just test` still work after Makefile deletion
- YAML syntax is valid
</verification>

<success_criteria>
- CI workflow updated with test+lint gates using just
- Makefile deleted from repository
- All just recipes continue to work
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-tooling-migration/07-02-SUMMARY.md`
</output>
