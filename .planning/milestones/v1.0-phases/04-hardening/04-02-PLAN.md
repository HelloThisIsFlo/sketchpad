---
phase: 04-hardening
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "A legitimate Claude AI request with a valid token and correct Origin continues to work normally after hardening"
    - "Claude Code test skill read/write cycle works post-hardening"
  artifacts: []
  key_links: []
---

<objective>
Verify that hardening did not break legitimate Claude AI traffic by re-running the E2E test skill and phone test.

Purpose: Security hardening is only valid if it blocks bad requests while still allowing good ones. This checkpoint confirms the happy path still works.
Output: Human-verified confirmation that Claude AI integration works post-hardening.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-hardening/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Claude AI integration works post-hardening</name>
  <files>none</files>
  <action>
Present the checkpoint to the user. Origin validation middleware has been deployed to the live server at sketchpad.kempenich.ai. Requests with bad Origin headers are now rejected with 403. Unauthenticated requests still return 401. Discovery endpoints remain open.

Ask the user to verify:

1. **Claude Code test skill**: Run `/test-sketchpad` in Claude Code CLI. It should successfully:
   - Read current sketchpad content
   - Write new test content with timestamp
   - Read back and verify the written content
   - Report PASS for all three steps

2. **Phone test** (optional but recommended): Open the MCP integration from Claude AI on your phone. Ask Claude to read and then write to your sketchpad. Both operations should complete normally.

3. **Expected behavior**: All read/write operations succeed exactly as they did before hardening. The Origin validation middleware should be transparent to legitimate clients (Claude Code CLI does not send Origin headers, so it passes through).

Wait for user response: "approved" or issue description.
  </action>
  <verify>User confirms "approved" — Claude Code test skill and phone test pass post-hardening</verify>
  <done>Legitimate Claude AI traffic works normally after hardening. No regression in MCP tool functionality (read_file, write_file). User confirms hardening is transparent to authorized clients.</done>
</task>

</tasks>

<verification>
- Claude Code test skill reports PASS on read, write, and read-back
- Phone test (if done) completes read and write successfully
</verification>

<success_criteria>
- Legitimate Claude AI traffic works normally after hardening
- No regression in MCP tool functionality (read_file, write_file)
- User confirms hardening is transparent to authorized clients
</success_criteria>

<output>
After completion, create `.planning/phases/04-hardening/04-02-SUMMARY.md`
</output>
