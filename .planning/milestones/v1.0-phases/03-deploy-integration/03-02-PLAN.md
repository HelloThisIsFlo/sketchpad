---
phase: 03-deploy-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - docs/01-synology-nfs.md
  - docs/02-github-oauth-app.md
  - docs/03-cloudflare-tunnel.md
  - docs/04-deploy.md
  - docs/05-claude-ai-setup.md
autonomous: true
requirements: [DOCS-01, DOCS-04]

must_haves:
  truths:
    - "docs/ folder has an index README.md with Quick Start at top and numbered guide list"
    - "Five numbered guides exist matching the setup sequence (01 through 05)"
    - "Existing docs are renamed with numeric prefixes preserving their content"
    - "GitHub OAuth App doc has corrected callback URL (/auth/callback not /github/callback)"
    - "Claude AI setup guide covers both CLI and phone with troubleshooting section"
  artifacts:
    - path: "docs/README.md"
      provides: "Index with Quick Start and guide list"
      contains: "Quick Start"
    - path: "docs/01-synology-nfs.md"
      provides: "Synology NFS guide (renamed)"
      min_lines: 10
    - path: "docs/02-github-oauth-app.md"
      provides: "GitHub OAuth App guide (renamed + corrected callback URL)"
      contains: "/auth/callback"
    - path: "docs/03-cloudflare-tunnel.md"
      provides: "Cloudflare Tunnel guide (renamed)"
      min_lines: 10
    - path: "docs/04-deploy.md"
      provides: "Kubernetes deployment guide (new)"
      contains: "make deploy"
    - path: "docs/05-claude-ai-setup.md"
      provides: "Claude AI integration setup for CLI and phone"
      contains: "phone"
  key_links:
    - from: "docs/README.md"
      to: "docs/01-synology-nfs.md through docs/05-claude-ai-setup.md"
      via: "markdown links in numbered guide list"
      pattern: "\\[.*\\]\\(0[1-5]-"
---

<objective>
Consolidate documentation under docs/ with numbered guides and a README index with Quick Start section.

Purpose: Fulfills DOCS-01 (docs/ folder with index and guides) and DOCS-04 (Claude AI setup guide for phone). Renames existing docs with numeric prefixes, fixes the callback URL error in the GitHub OAuth doc, and creates two new guides (deploy and Claude AI setup).

Output: docs/README.md, five numbered guides (01-05), existing supplementary docs kept as-is
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deploy-integration/03-CONTEXT.md
@.planning/phases/03-deploy-integration/03-RESEARCH.md

<interfaces>
<!-- Existing docs to rename. Content preserved, just renamed with numeric prefix. -->

Current docs/ files:
- synology-nfs.md       -> 01-synology-nfs.md (git mv)
- github-oauth-app.md   -> 02-github-oauth-app.md (git mv + fix callback URL)
- cloudflare-tunnel.md  -> 03-cloudflare-tunnel.md (git mv)
- google-oauth-app.md   (stays as supplementary, no rename)
- local-development.md  (stays as supplementary, no rename)
- mcp-inspector.md      (stays as supplementary, no rename)

From 03-RESEARCH.md - Callback URL fix:
```
Phase 1 doc says: /github/callback (WRONG)
FastMCP actually uses: /auth/callback (CORRECT)
Fix in 02-github-oauth-app.md: change all occurrences of /github/callback to /auth/callback
```

From 03-RESEARCH.md - Claude Code CLI command:
```bash
claude mcp add --transport http sketchpad https://sketchpad.kempenich.ai/mcp
```

From 03-RESEARCH.md - Claude.ai web connector:
```
1. Go to https://claude.ai/settings/connectors
2. Click "Add custom connector"
3. Enter URL: https://sketchpad.kempenich.ai/mcp
4. Complete OAuth flow in browser (GitHub login)
5. Connector syncs automatically to Claude mobile apps
```

From 03-RESEARCH.md - Makefile targets for Quick Start:
```
make build    # Build container image
make push     # Push to ghcr.io
make deploy   # Apply K8s manifests + wait for rollout
make all      # Build + push + deploy
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename existing docs and fix callback URL</name>
  <files>docs/01-synology-nfs.md, docs/02-github-oauth-app.md, docs/03-cloudflare-tunnel.md</files>
  <action>
Use `git mv` to rename the three existing docs with numeric prefixes:
```bash
git mv docs/synology-nfs.md docs/01-synology-nfs.md
git mv docs/github-oauth-app.md docs/02-github-oauth-app.md
git mv docs/cloudflare-tunnel.md docs/03-cloudflare-tunnel.md
```

Then fix the callback URL in `docs/02-github-oauth-app.md`:
- Replace ALL occurrences of `/github/callback` with `/auth/callback`
- This includes the table row, the verification checklist, and the notes section
- The correct callback URL is `https://sketchpad.kempenich.ai/auth/callback`
- FastMCP uses `/auth/callback` as its OAuth callback path, not `/github/callback`

Do NOT rename google-oauth-app.md, local-development.md, or mcp-inspector.md -- they stay as supplementary docs without numeric prefixes.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Agentic/Capacities/sketchpad && test -f docs/01-synology-nfs.md && test -f docs/02-github-oauth-app.md && test -f docs/03-cloudflare-tunnel.md && ! test -f docs/synology-nfs.md && ! test -f docs/github-oauth-app.md && ! test -f docs/cloudflare-tunnel.md && ! grep -q '/github/callback' docs/02-github-oauth-app.md && grep -q '/auth/callback' docs/02-github-oauth-app.md && echo 'PASS'</automated>
  </verify>
  <done>Three docs renamed with numeric prefixes. Old names gone. Callback URL corrected from /github/callback to /auth/callback in 02-github-oauth-app.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create new docs (04-deploy, 05-claude-ai-setup, README index)</name>
  <files>docs/04-deploy.md, docs/05-claude-ai-setup.md, docs/README.md</files>
  <action>
Create `docs/04-deploy.md` -- Kubernetes deployment guide:
- Title: "Deploying to Kubernetes"
- Prerequisites: kubectl configured, namespace exists, Secrets created, PVCs bound, image pushed to ghcr.io
- Sections: Build and Push (Makefile targets), Deploy (make deploy), Verify (kubectl get pods, curl health endpoint, curl OAuth discovery)
- Include: deleting the placeholder first (`kubectl delete deployment sketchpad-placeholder -n sketchpad` and `kubectl delete configmap placeholder-config -n sketchpad`)
- Include: how to check logs (`kubectl logs -f deployment/sketchpad -n sketchpad`)
- Include: updating image after code changes (push to main triggers CI, then `make deploy` to apply)
- Keep concise -- this is a reference for someone who knows K8s basics

Create `docs/05-claude-ai-setup.md` -- Claude AI integration setup (per user decision: brief steps, covers both CLI and phone, includes troubleshooting):
- Title: "Setting Up Claude AI Integration"
- Section 1: "Claude Code CLI" -- `claude mcp add --transport http sketchpad https://sketchpad.kempenich.ai/mcp`, then `/mcp` to authenticate, complete GitHub login in browser
- Section 2: "Claude.ai (Phone)" -- go to claude.ai/settings/connectors, add custom connector with URL `https://sketchpad.kempenich.ai/mcp`, complete OAuth, connector syncs to mobile apps
- Section 3: "Verify" -- ask Claude to read the sketchpad, write something, read it back
- Section 4: "Troubleshooting" -- common issues: OAuth redirect mismatch (check GitHub App callback URL is /auth/callback), about:blank in browser (known bug #11814, use Claude Code CLI instead), 502/503 errors (pod not running, check `kubectl get pods`), "No tools available" (OAuth not completed, run `/mcp` and authenticate)
- Keep brief per user decision -- steps not tap-by-tap instructions

Create `docs/README.md` -- index with Quick Start at top (per user decision):
- Title: "Sketchpad Documentation"
- Section 1: "Quick Start" -- all commands in order for someone who's done it before: the essential make/kubectl/claude commands with no explanation, just the commands
- Section 2: "Guides" -- numbered list linking to all five guides with one-line descriptions
- Section 3: "Supplementary" -- links to google-oauth-app.md, local-development.md, mcp-inspector.md with brief descriptions
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Agentic/Capacities/sketchpad && test -f docs/README.md && test -f docs/04-deploy.md && test -f docs/05-claude-ai-setup.md && grep -q 'Quick Start' docs/README.md && grep -q 'phone' docs/05-claude-ai-setup.md && grep -qi 'troubleshoot' docs/05-claude-ai-setup.md && ls docs/0[1-5]-*.md | wc -l | grep -q '5' && echo 'PASS'</automated>
  </verify>
  <done>docs/README.md has Quick Start + guide list. docs/04-deploy.md covers K8s deployment. docs/05-claude-ai-setup.md covers CLI + phone + troubleshooting. All 5 numbered guides exist.</done>
</task>

</tasks>

<verification>
- `docs/README.md` exists with "Quick Start" section and links to all 5 numbered guides
- `ls docs/0[1-5]-*.md | wc -l` returns 5
- `docs/02-github-oauth-app.md` contains `/auth/callback` (not `/github/callback`)
- `docs/05-claude-ai-setup.md` mentions "phone" and "troubleshoot"
- Supplementary docs (google-oauth-app.md, local-development.md, mcp-inspector.md) still exist unchanged
</verification>

<success_criteria>
docs/ folder is fully consolidated: README index with Quick Start, five numbered guides in sequence, callback URL fixed, Claude AI setup covers both CLI and phone with troubleshooting. Supplementary docs preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deploy-integration/03-02-SUMMARY.md`
</output>
