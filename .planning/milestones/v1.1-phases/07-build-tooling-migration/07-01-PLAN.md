---
phase: 07-build-tooling-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Justfile
  - src/sketchpad/__init__.py
  - src/sketchpad/__main__.py
  - src/sketchpad/config.py
  - src/sketchpad/middleware.py
  - src/sketchpad/server.py
  - src/sketchpad/tools.py
  - src/sketchpad/user_identity.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_storage_limits.py
  - tests/test_user_identity.py
  - tests/test_user_isolation.py
autonomous: true
requirements:
  - BUILD-01

must_haves:
  truths:
    - "`just --list` shows all expected recipes: build, deploy, restart, status, test, lint, fmt, dev, logs"
    - "`just test` runs pytest and all existing tests pass"
    - "`just lint` passes with zero violations on the formatted codebase"
    - "All Python source files are ruff-formatted (no diff on `ruff format --check .`)"
  artifacts:
    - path: "Justfile"
      provides: "Command runner replacing Makefile"
      contains: "IMAGE := \"ghcr.io/hellothisisflo/sketchpad\""
    - path: "pyproject.toml"
      provides: "Ruff dev dependency and configuration"
      contains: "[tool.ruff]"
  key_links:
    - from: "Justfile"
      to: "uv run pytest"
      via: "test recipe"
      pattern: "uv run pytest"
    - from: "Justfile"
      to: "uv run ruff check"
      via: "lint recipe"
      pattern: "uv run ruff check"
    - from: "Justfile"
      to: "uv run ruff format"
      via: "fmt recipe"
      pattern: "uv run ruff format"
---

<objective>
Add ruff as linter/formatter with a one-time formatting pass, then create the Justfile with all recipes (4 translated from Makefile + 5 new dev recipes).

Purpose: Establishes the Justfile as the project's command runner and normalizes code formatting so future lint checks pass cleanly.
Output: Working Justfile, ruff-formatted codebase, ruff config in pyproject.toml
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-tooling-migration/07-CONTEXT.md
@.planning/phases/07-build-tooling-migration/07-RESEARCH.md

@Makefile
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ruff config and run one-time format</name>
  <files>pyproject.toml, src/sketchpad/__init__.py, src/sketchpad/__main__.py, src/sketchpad/config.py, src/sketchpad/middleware.py, src/sketchpad/server.py, src/sketchpad/tools.py, src/sketchpad/user_identity.py, tests/__init__.py, tests/conftest.py, tests/test_storage_limits.py, tests/test_user_identity.py, tests/test_user_isolation.py</files>
  <action>
1. Edit `pyproject.toml`:
   - Add `"ruff>=0.15"` to the `[project.optional-dependencies] dev` list (alongside existing `"pytest>=8.0"`)
   - Add ruff configuration sections:
     ```toml
     [tool.ruff]
     target-version = "py312"
     line-length = 88

     [tool.ruff.lint]
     select = ["E4", "E7", "E9", "F", "B", "I"]

     [tool.ruff.format]
     quote-style = "double"
     ```

2. Run `uv sync --dev` to install ruff into the venv.

3. Run `uv run ruff format .` to normalize all Python files (src/ and tests/). This is the one-time formatting commit per user decision.

4. Run `uv run ruff check .` to verify lint passes on the freshly formatted code. Fix any violations if found (unlikely with these rule sets on a small codebase, but handle if present).

5. Run `uv run pytest -x` to confirm existing tests still pass after formatting.

6. Commit all changes as a single formatting commit: "style(07-01): add ruff config and format all Python files"
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Agentic/Capacities/sketchpad && uv run ruff format --check . && uv run ruff check . && uv run pytest -x</automated>
  </verify>
  <done>pyproject.toml has ruff dev dependency and [tool.ruff] config. All Python files pass `ruff format --check` and `ruff check`. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create Justfile with all recipes</name>
  <files>Justfile</files>
  <action>
Create `Justfile` at project root with the following content. Use the exact Justfile syntax from the research — backtick expressions for shell commands, `{{VAR}}` interpolation, `@` prefix to suppress echo where appropriate.

**Project constants (top of file):**
```
IMAGE := "ghcr.io/hellothisisflo/sketchpad"
NS    := "sketchpad"
SHA   := `git rev-parse --short HEAD`
TAG   := "sha-" + SHA
```

**Recipes to include (9 total):**

1. `default` — runs `@just --list` (safe, discoverable, per user decision)
2. `build` — `docker buildx build --platform linux/amd64 -t {{IMAGE}}:{{TAG}} -t {{IMAGE}}:latest --load .` (preserves --platform flag from Makefile per pitfall #4)
3. `deploy` — `kubectl apply -f k8s/deployment.yaml -f k8s/service.yaml -n {{NS}}` then `kubectl rollout status deployment/sketchpad -n {{NS}} --timeout=120s` (assumes CI already pushed image per user decision)
4. `restart` — `kubectl rollout restart deployment/sketchpad -n {{NS}}` then `kubectl rollout status deployment/sketchpad -n {{NS}} --timeout=120s`
5. `status` — `kubectl get pods -n {{NS}}` then `kubectl get svc -n {{NS}}`
6. `test` — `uv run pytest`
7. `lint` — `uv run ruff check .`
8. `fmt` — `uv run ruff format .`
9. `dev` — `uv run python -m sketchpad` (local dev server)
10. `logs` — `kubectl logs -f -l app=sketchpad -n {{NS}} --tail=100`

Group recipes with comment headers: Build, Dev, K8s. Add a short comment above each recipe describing what it does.

Do NOT include `push` or `all` recipes (removed per user decision).

After creating, run `just --list` to verify all recipes appear. Then run `just test` to verify it wraps pytest correctly. Then run `just lint` to verify it wraps ruff correctly.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Agentic/Capacities/sketchpad && just --list && just test && just lint</automated>
  </verify>
  <done>`just --list` shows all 9 recipes (default, build, deploy, restart, status, test, lint, fmt, dev, logs). `just test` runs pytest successfully. `just lint` passes with zero violations.</done>
</task>

</tasks>

<verification>
- `just --list` output contains: build, deploy, restart, status, test, lint, fmt, dev, logs
- `just test` exits 0 (all tests pass)
- `just lint` exits 0 (no ruff violations)
- `uv run ruff format --check .` exits 0 (all files formatted)
- `uv run pytest -x` exits 0 (tests unbroken by formatting)
</verification>

<success_criteria>
- Justfile exists with all 9 recipes producing correct output
- Ruff is configured in pyproject.toml and installed as dev dependency
- All Python files are ruff-formatted in a dedicated formatting commit
- Existing test suite passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-tooling-migration/07-01-SUMMARY.md`
</output>
